.name
  .links
    = link_to project.name, project, :class => "title"
    - if can? :manage, Project
      = link_to image_tag("icons/pencil.png", :title => "Edit"), edit_project_path(project)

  .image
    = link_to project do
      = random_logo(project)

= render "language_statistics", :project => project