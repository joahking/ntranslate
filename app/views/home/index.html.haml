.home-content

  %h1.title Translations made the easy way

  #slider
    %ul
      %li
        = image_tag("projects.png", :class => "left", :size => "910x440")
      %li
        = image_tag("project_detail.png", :class => "left", :size => "910x440")
      %li
        = image_tag("text_resorces.png", :class => "left", :size => "910x440")
      %li
        = image_tag("translate.png", :class => "left", :size => "910x440")

  %p
    %span Manage all your projects
    = image_tag("projects.png", :class => "left", :size => "150x100")
    Easily create a new project to manage the translations for your Rails project.
    On the project page you can quickly view the current status of the pending translations in your project.
  %p
    %span Import and export translations
    = image_tag("text_resorces.png", :class => "left", :size => "150x100")
    Create new texts for your project one by one or do a bulk import by uploading a YAML file.
    When everything is done you can just as easily export to YAML or JSON with one click!
  %p
    %span Translate in a breeze
    = image_tag("translate.png", :class => "left", :size => "150x100")
    Navigate through a project's pending translations and comfortably
    translate with the help of Bing Translator.

:javascript

$(document).ready(function(){
  $("#slider").easySlider({
    auto: true,
    continuous: true,
    speed: 700,
    pause: 3000
  });
});
